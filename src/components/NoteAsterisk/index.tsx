import { AsteriskIcon } from '../../icons/Asterisk';

export const NoteAsterisk = () => {
  return (
    <AsteriskIcon
      width="0.75rem"
      height="0.75rem"
      sx={{ width: '0.75rem', height: '0.75rem', color: 'var(--ifm-color-primary)' }}
    />
  );
};
